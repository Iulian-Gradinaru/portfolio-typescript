/**
 * Imports Project interface
 */
import { Project as ProjectStructure } from '../utils/projects';

/**
 * Defines the props interface
 */
export interface ProjectProps {
  project: ProjectStructure;
}

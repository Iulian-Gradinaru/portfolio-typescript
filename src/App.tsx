import { Fragment } from 'react';
import { Routes } from './components/Routes';
import './App.css';

export const App: React.FC = () => {
  return (
    <Fragment>
      <Routes />
    </Fragment>
  );
};
